# 운영판 0.2 검증 결과 — 2026-09-13

기준은 `Public_Office_Jobs_v0.1_auto_refresh.zip`입니다. 이전 route-check와 후속 PDF·GRAC 충돌 보완을 보존했습니다. 데이터는 **13 → 14개 공고, 15 → 16개 모집단위**이며 현재 공고 9건·지난 공고 5건입니다.

## 실제 원격 실행

2026-09-13 02:11:21–02:12:35 UTC에 기존 수집 경로로 총 **6회** 요청했습니다. 목록 1·3페이지, 상세 304654·304899·301038·300568입니다. 재시도·첨부 요청은 0회, 전체 수신량은 270,040바이트입니다. HTTP 200만 확인한 것이 아니라 실제 목록행, 기관·공고 ID, 상세 본문 구조와 원문 해시를 검사했습니다.

- 신규: **과학기술정책연구원 304654**, ‘시스템혁신(행정직)’ 1개 모집단위를 AI 규칙 없이 추가했습니다. 공고 전체 7명을 이 행정 모집인원으로 쓰지 않았으며 단위 모집인원·급여·세부 자격은 미확인입니다.
- 재확인: 세계김치연구소 304899, 영상물등급위원회 301038, 국립중앙의료원 300568. 저장된 현재·마감 공고가 모두 실제 상세 요청 대상이 됐습니다.
- 결과: 304899와 300568의 저장 단위명과 새 공식 표 명칭을 정확히 대응하지 못해 **부분 성공**으로 기록했습니다. 300568의 기존 공식 8:1 등 정상 자료는 보존하고 새 표와 연결되지 않았음을 표시합니다.
- 301038의 공식 34:1 및 전형별 인원을 확인했습니다. 기존 공고명과 공식 본문의 기관명 접두사 차이는 이전 원문이 없는 상태에서 확인한 메타데이터 차이입니다. 실제로 조건이 정정됐다고 단정하지 않았습니다.
- 이번 실제 원격 표본에서 새로운 경쟁률 발표·수치 수정은 확인하지 못했습니다. 추가·수정 처리는 아래 가상 시험으로 검증했습니다.

## 저장 원문 재생

실제 실행으로 발견한 제목 차이 처리 문제를 고친 뒤 **같은 6개 원문을 최종 코드로 재생**했습니다. 추가 원격 요청은 없습니다. 최종 배포 데이터의 실행 방식은 이를 반영해 `replay`로 기록했습니다. 최초 실행 기록을 지우거나 최신 코드로 원격 실행한 것처럼 바꾸지 않았습니다.

같은 묶음을 두 번 재생해 공고 14건·모집단위·필드값·검토시각·대기열·다음 페이지가 중복되거나 달라지지 않음을 확인했습니다. 최초 해시 기준 수립/다음 비교 같은 실행 메타데이터는 구분합니다. 기존 13개 공고의 항목 값과 기존 검토시각을 모두 보존했습니다. 새로 본 원문이 없는 기존 수작업 조건은 값을 유지하면서 현재 적용 여부를 ‘일부 확인’으로 표시했습니다. 다음 목록 위치는 **4페이지**, 상세 대기 공고는 **24건**입니다.

## 오프라인·가상 자료 시험

Python **112개**, JavaScript **51개** 검증을 통과했습니다. 가상 공고·변경 수치는 시험용 임시 데이터에만 사용했고 운영 JSON에 넣지 않았습니다.

- 경쟁률 신규 발표·수치 정정·전형별 인원 수정, 기존 보수·컴활·모집인원 보존.
- 모집단위 불일치, 경쟁률·전형 인원 공란 후퇴, 파싱 실패 시 이전 공식 결과 보존.
- 10페이지를 넘는 목록 이동, 다음 실행 큐 복원, 처리 전에 마감된 후보 보존, 한국 날짜 기준 마감 재확인 주기.
- UTF-8 읽기·쓰기 및 CP949 기본값 모사, 중복 실행 잠금, 쓰기 오류 복원.
- 로컬 bare Git 저장소에 실제 커밋·push·다시 읽기, 동일 자료 재저장, push 충돌 시 강제 덮어쓰기와 배포 차단, 원문·로그의 커밋 제외.
- 목록·검색·지역·연도 필터·상세·지난 공고 이동, 캐시 우회 JSON 재조회·잘못된 응답 시 기존 게시본 표시를 **JavaScript VM DOM 시험**으로 확인.
- PC·모바일용 CSS 전환·viewport·폼 라벨의 소스 확인. **실제 브라우저의 PC·모바일 화면 배치·터치 조작은 미검증**입니다. 이전 환경의 브라우저 미리보기 접근 제한을 우회하지 않았습니다.

배포 ZIP을 별도 임시 폴더에 풀어 원문·AI 규칙 파일이 없는 상태에서 공개 패키지용 Python 23개·JavaScript 16개 검사와 빌드를 다시 통과했습니다(위 전체 검사에 포함된 검사를 패키지에서도 확인). 공개물은 HTML·JSON·`.nojekyll` 3개 파일뿐입니다. 추가로 실제 로컬 HTTP 서버에 화면의 데이터 조회 함수를 연결해 JSON 변경 뒤 최신 값 수신, 잘못된 JSON 응답 시 이전 자료 보존을 확인했습니다. 이 3회 로컬 요청은 JOB-ALIO 요청이 아니며 브라우저 화면 렌더링 검사도 아닙니다.

## 배포와 예약 상태

GitHub workflow 파일·저장 커밋·Pages 공개물 생성까지 준비했습니다. **계정/저장소 업로드, GitHub Actions 원격 실행, Pages 배포 URL, 배포 환경의 JOB-ALIO 접근, 실제 schedule 실행은 아직 검증하지 않았습니다.** 예약은 6시간 cron 정의만 작성했고 활성화 변수는 기본 꺼짐입니다. 계정·저장소·공개 범위 승인 뒤 수동 실행을 먼저 확인해야 합니다.

공개 패키지는 원문·첨부·개발 전체 로그·AI 규칙을 제외한 허용 파일 목록으로 만들었습니다. 사이트에는 HTML과 공개 JSON만 올립니다. 공개 저장소를 선택하면 저장소 코드·운영 JSON·Git 이력도 공개됩니다. 자료의 개인 식별정보나 비밀키를 추가하지 마세요.

별도 검증기록 ZIP은 공개 업로드 대상이 아닙니다. 이 파일에는 실제 6회 원문과 최종 재생 기록, 시험 로그를 담았습니다. 운영에는 필요하지 않습니다.
